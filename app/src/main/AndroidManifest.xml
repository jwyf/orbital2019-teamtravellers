<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.travelinpeace">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NewItineraryActivity"></activity>
        <activity android:name=".UpdatePasswordActivity" />
        <activity android:name=".UpdateProfileActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".ResetPassActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".EditorDay1" />
        <activity android:name=".ReminderDetails" />
        <activity android:name=".ReminderActivity" />
        <activity android:name=".ExistingTripsDetails" />
        <activity android:name=".ExistingTripsActivity" />
        <activity android:name=".DayDetail" />
        <activity android:name=".DayDetailExisting" />
        <activity android:name=".WeekActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>